# Tambien son arrays xd todo se define como arrray pero se pueden guardar todos los tipos
numeros = [2, "fasf", 435]
print(numeros[1])
matrix = [[2, 3], [4, 5], [2]]  # El anidamiento es igual q en js
print(matrix[2])
ceros = [0, 1]*10  # Incluye en el array los datos 10 veces
print(ceros)
# Se pueden sumar los arrays Wtf???? Es como el concat de js
numerosmat = numeros + matrix
print(numerosmat)
# Devuelve una listad el 1-10 range toma dos params el inicio y el fin
rango = list(range(1, 11))
print(rango)
# Itera sobre el string y devuelve un array de caracteres
casa = list("asfasfasf")
print(casa)
